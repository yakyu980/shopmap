# חוקי עיצוב — SuperNav AI

מסמך זה מגדיר את חוקי העיצוב הקיימים בפועל באפליקציה, כפי שהם מיושמים
היום ב-`src/index.css` וב-`src/App.css`. המטרה: כל דף/רכיב חדש ישתמש
**באותם טוקנים ותבניות** של הדפים הקיימים, כדי שהממשק יישאר אחיד ולא
ייווצרו וריאציות-צבע/מרווח מקומיות פר-קומפוננטה.

## 1. טוקנים (`src/index.css`, `:root`)

כל צבע/רדיוס בממשק **חייב** לבוא מהמשתנים האלה — אסור לקבע (hardcode)
צבע חדש ברכיב, אלא אם זהו צבע-סטטוס נקודתי (ר' סעיף 5).

| משתנה | תפקיד |
|---|---|
| `--bg` | רקע-עמוד |
| `--surface` | רקע כרטיס/שורה/מודל |
| `--text` | טקסט ראשי |
| `--text-dim` | טקסט משני (מיקום, הסבר, timestamps) |
| `--primary` | כחול-מסלול — פעולה ראשית, טאב פעיל |
| `--primary-dark` | נייבי — כותרות, ערכי-מספר בולטים, לינק/טקסט-פעולה |
| `--primary-tint` | רקע עדין למצב-פעיל/הדגשה (לא לטקסט) |
| `--secondary` | ירוק — **אך ורק** הצלחה/אישור/"הזול ביותר", לא לזהות-מותג |
| `--danger` | אדום — מחיקה, שגיאה, "לא נמצא" |
| `--border` | קו-מתאר לכל קופסה |
| `--radius` | 14px — רדיוס ברירת-מחדל לכרטיסים/מפה |

לכל המשתנים יש הגדרה מקבילה תחת `@media (prefers-color-scheme: dark)`
— **כל טוקן חדש שנוסף ל-`:root` חייב לקבל גם ערך-דארק-מוד תואם** באותו
בלוק, אחרת המצב הכהה ישבר.

**RTL:** `body { direction: rtl }` גלובלי. אין להוסיף `direction: ltr`
נקודתי אלא לתוכן שחייב זאת (מספרי-ברקוד, קוד-משפחה).

## 2. מבנה-עמוד וריווח

- כל עמוד עוטף תוכן ב-container ברוחב מקסימלי `480px` (`.app`) —
  אפליקציה היא mobile-first, לא מיועדת לדסקטופ רחב. **כל תצוגה
  חדשה — עמוד, מודל, כרטיס — חייבת להיבדק/להתאים למסך-טלפון (רוחב
  ~360–480px)**: בלי גלילה אופקית, בלי טקסט/כפתור שנחתך בקצה, יעדי-
  מגע (כפתורים/צ'יפים) לא קטנים מדי לאצבע. אין layout שמניח עכבר/hover
  כדרך-האינטראקציה היחידה.
- Padding אחיד לעמוד-תוכן: `12px 16px 24px` (ר' `.home-page`,
  `.compare-page`) — עמוד חדש שמתווסף כטאב יורש את אותו padding, לא
  ממציא ריווח משלו.
- ריווח פנימי בין בלוקים בתוך עמוד: `gap: 8–14px` (`flex-direction: column`).
- כרטיסים/רשימות פנימיות: `gap: 6px` בין items.

## 3. כרטיסים, שורות ורשימות

תבנית-כרטיס אחידה בכל מקום (`product-row`, `cart-row`, `nav-item`,
`compare-row`, `deal-row`, `favorite-row`...):

```css
background: var(--surface);
border: 1px solid var(--border);
border-radius: 10–12px;  /* var(--radius) = 14px לכרטיסים גדולים יותר */
padding: 6–10px;
```

רשימות הן תמיד `<ul>` עם `list-style: none; margin: 0; padding: 0;
display: flex; flex-direction: column; gap: 6px` — לא `<div>` עם
מרווחים ידניים בין items.

שורה עם שם+מיקום/מחיר משתמשת בתבנית הקבועה: `*-row-icon` (20px) →
`*-row-info` (flex column: שם מודגש 14px + שורת-פרטים 12px
`--text-dim`) → `*-row-price`/badge בקצה. רכיב חדש שמציג "פריט עם
שם+מיקום+מחיר" צריך לעקוב אחרי התבנית הזו, לא לבנות layout חדש.

## 4. כפתורים (`.btn`)

מחלקת-בסיס אחת (`.btn`) + מודיפייר — **אין ליצור כפתור מאפס בקומפוננטה
חדשה**:

- `.btn--primary` — פעולה ראשית יחידה בכל מסך/מודל (כחול, טקסט לבן).
- `.btn--ghost` — פעולה משנית (רקע `--surface` + border).
- `.btn--text` — פעולה טקסטואלית קלה (ללא רקע).
- `.btn--icon` — כפתור-ריבועי 34×34 לאייקון בלבד.
- `.btn--small` — גרסה מכווצת (צ'יפים/פעולות-משניות בתוך שורה).
- `.btn--danger` — טקסט אדום, לפעולת מחיקה/הסרה.

כפתור ראשי-של-מסך (למשל "סיים קנייה", "שמור קבלה") הוא `width: 100%`
(ר' `.nav-next`, `.receipt-save-btn`, `.modal-add`) — לא ממורכז/מוצמד
לתוכן.

## 5. תגי-סטטוס וצבע נקודתי

צבעי-רקע-בהירים (amber/red/indigo) שאינם טוקן גלובלי משמשים **רק**
לסוג-הודעה קבוע, ותמיד עם זוג ערך-דארק-מוד תואם באותו קובץ:

- ענבר (`#fef3c7`/`#92400e`, דארק `#3a2c10`/`#fcd34d`) — אזהרה/רמז
  שדורש תשומת-לב לא-דחופה (`map-edit-hint`, `step-hint`,
  `modal-flag`).
- אדום-בהיר (`#fef2f2`/`#b91c1c`, דארק `#2a1414`/`#fca5a5`) — מבצע/הנחה
  (`sale-banner`, `price-tag-percent`).
- אינדיגו-בהיר (`#eef2ff`, דארק `#1e2340`) — תובנה/חיזוי לא-דחוף
  (`predictions-panel`, `map-move-hint`).

אל תמציא צבע-סטטוס רביעי בלי סיבה — אם ההודעה החדשה היא "אזהרה" השתמש
בענבר הקיים, אם "הנחה/הצלחה" השתמש באדום/ירוק הקיימים.

## 6. מודלים ממורכזים

**כל פיצ'ר/יכולת חדשה שלא פותחת דף-טאב משלה** (לא אחד מ-5 הטאבים
הראשיים) **חייבת להופיע כ-modal ממורכז באמצע המסך** — לא bottom-sheet,
לא inline בתוך זרימת-הדף הקיימת. כל מודל בנוי לפי אותה תבנית
(`modal-backdrop` + `modal`, `settings-overlay` + `settings-panel`):
backdrop כהה למחצה (`rgba(0,0,0,0.45)`, `position: fixed; inset: 0`),
ממורכז אנכית-ואופקית (`display: flex; align-items: center;
justify-content: center; padding: 20px`), הפאנל עצמו `border-radius:
20px` (כל הפינות, לא רק העליונות), `max-height: 88vh; overflow-y:
auto`, וכפתור-סגירה עגול בפינה (`.modal-close`, 30×30). ה-`padding:
20px` על ה-backdrop הוא קריטי-למובייל — בלעדיו המודל נוגע בקצוות
המסך במכשירים צרים. מודל חדש **לא** בונה backdrop/סגירה/מיקום משלו —
משתמש בתבנית הזו בדיוק.

## 7. אייקונים

אין אמוג'ים ואין ספריית-אייקונים חיצונית. כל אייקון הוא `<Icon
name="..."/>` שמפנה ל-sprite אחד (`IconSprite.jsx`). גודל ברירת-מחדל
`1em` (יורש מהטקסט הסובב) כדי שייכנס לכל כפתור/כותרת בלי override; גודל
נקודתי (כמו `.tab .icon { width:18px }`) נקבע רק כשהאייקון עומד לבד ולא
לצד טקסט. הוספת אייקון חדש דורשת סמל חדש ב-sprite, לא ייבוא ספריה.

## 8. Empty states והודעות-הסבר

טקסט-הסבר קצר (`text-dim`, 11–13px) מתחת לכותרת/רכיב — לא tooltip,
לא modal נפרד — כשצריך להבהיר "מה זה" או "מה קורה אם ריק"
(`.empty-hint`, `.compare-intro`, `.family-disclaimer`,
`.settings-hint`). רשימה ריקה מציגה משפט ממורכז ב-`--text-dim`, לא
מסך ריק לגמרי.

## 9. עקביות בין דפים

לפני הוספת מסך/רכיב חדש: לחפש אם כבר קיימת תבנית דומה (שורת-מוצר,
צ'יפ, מודל, כרטיס-סטטיסטיקה) ולהשתמש באותה מחלקת-CSS/מבנה, במקום
ליצור וריאציה חדשה. שמות-מחלקות חדשים עוקבים אחרי המוסכמה הקיימת:
`<page-or-feature>-<part>` (למשל `compare-row`, `venue-row-btn`), עם
מודיפייר בסיומת `--state` (`--active`, `--picked`, `--cheapest`).
