import { useMappingSession } from '../lib/useMappingSession'; import Icon from './Icon';
export default function MappingSessionPanel() { const s = useMappingSession(); return <section className="mapping-session" aria-label="סשן מיפוי"><div><strong>סשן מיפוי ניסיוני</strong><small>הקלטת תנועה לבדיקה; עדיין לא מחשב מיקום בתוך החנות</small></div><div className="mapping-session__actions">{s.active ? <button className="btn btn--ghost btn--small" type="button" onClick={s.stop}><Icon name="stop" /> עצור</button> : <button className="btn btn--primary btn--small" type="button" onClick={s.start}><Icon name="play" /> התחל הקלטה</button>}<button className="btn btn--ghost btn--small" type="button" onClick={s.download} disabled={!s.samples.length}><Icon name="download" /> הורד JSON</button></div><small className="mapping-readiness__privacy">{s.active ? `מקליט עכשיו · ${s.samples.length} דגימות` : `${s.samples.length} דגימות בזיכרון הדף`}</small></section>; }
