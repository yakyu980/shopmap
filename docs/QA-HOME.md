# דוח QA — דף הבית

תאריך בדיקה: 2026-09-01
גרסה שנבדקה: `9a6fe76` + שינויים מקומיים במסמך `docs/HOME.md`
סביבת בדיקה: Vite מקומי ב־`http://127.0.0.1:5173/`, production build עבר בנפרד.

## סיכום מנהלים

דף הבית נטען לאחר התחברות ומכיל את רוב פעולות הרשימה הבסיסיות. יש גם כפתור מצלמה מרכזי לסריקת ברקוד או לזיהוי מוצר בתמונה. עם זאת, דף הבית הקבוצתי כפי שהוגדר בדרישות עדיין לא ממומש: הקבוצות נמצאות בתוך פרופיל, ואין בחירת קבוצה שמחליפה את הרשימה האישית ואת הקונטקסט של המסך.

## מטריצת בדיקות

| דרישה | מצב | ראיה | הערה |
|---|---|---|---|
| התחברות עם סיסמה | ✅ קיים | `AuthGate.jsx`, `Login.jsx`, `auth.js` | נבדק חי: שדות משתמש/סיסמה וכפתור התחברות מופיעים. לא בוצעה התחברות בלי חשבון בדיקה. |
| הרשמה | ✅ קיים | `Login.jsx`, `auth.js` | כוללת שאלת ותשובת אבטחה. |
| שכחתי סיסמה | ✅ קיים | `ForgotPassword` בתוך `Login.jsx` | מבוסס על שאלת אבטחה ואיפוס דרך API. |
| תמונת פרופיל | ✅ קיים | `UserPanel.jsx`, `updateProfilePhoto` | בחירת קובץ ושליחה לשרת; דורש משתמש מחובר. |
| יצירת קבוצה | ✅ קיים חלקית | `UserPanel.jsx`, `groups.js`, `server/routes/groups.js` | הקבוצה נוצרת, אך אין מעבר אוטומטי לדף בית קבוצתי. |
| הזמנה/הצטרפות לקבוצה | ✅ קיים | `createInvite`, `joinGroup`, `?join=TOKEN` | קישור נוצר למנהל והצטרפות מתבצעת בפרופיל/בפתיחת קישור. |
| בחירת פרופיל קבוצה כקונטקסט | ✅ קיים | `App.jsx`, `UserPanel.jsx`, `Home.jsx` | בחירה בפרופיל קבוצה מחליפה את דף הבית למצב קבוצה. |
| רשימה קבוצתית כמו WhatsApp | ✅ קיים בסיס | `useGroupHome.js`, `/groups/:id/home` | רשימה קבועה לפי קבוצה עם polling של 5 שניות; עדיין לא WebSocket בזמן אמת. |
| זיהוי מי הוסיף מוצר | ✅ חלקי | `trip.items[].addedBy`, מוצג ב־`Home.jsx` | קיים רק במצב trip. |
| מנהל/חבר/מוגבל | ✅ קיים | `UserPanel.jsx`, `server/routes/groups.js` | תפקידים והגבלות נשמרים בשרת. |
| מנהל נוסף | ✅ קיים | `updateMember` עם `role: admin` | מוגן בצד שרת מפני הורדת המנהל האחרון. |
| הגבלת קטגוריות | ✅ קיים | `canAddProduct` ב־`server/routes/groups.js` | נאכף בהוספה ובהעברה לרשימת קבוצה. |
| הגבלת מוצר יחיד/מצב ילד | ✅ קיים | `canAddProduct` ב־`server/routes/groups.js` | נאכף בהוספה ובהעברה לרשימת קבוצה. |
| הגבלת כמות מוצרים | ❌ חסר | אין שדה/route/בדיקת quantity בהגבלות | הדרישה הוגדרה אך לא קיימת במודל הנוכחי. |
| שיתוף/העברה מאישי לקבוצה | ✅ קיים בסיס | `Home.jsx`, `POST /groups/:id/home/items/import` | העברה אצווה קיימת; פריטים שנדחו מוחזרים. |
| בחירת סניף | ✅ קיים חלקית | `TripVenuePicker.jsx`, `venues.js` | בחירת הסניף משויכת לפתיחת trip, לא לכל מצב של דף בית קבוצתי. |
| חיפוש מוצר | ✅ קיים | `searchCatalog` + תוצאות checkbox ב־`Home.jsx` | חיפוש לפי תחילת שם בלבד. |
| הוספה קולית | ✅ קיים | `VoiceAddPanel.jsx`, `useSpeechRecognition.js` | התאמה נעשית מול הקטלוג הסטטי. |
| סריקת ברקוד במצלמה | ✅ קיים בכפוף לסביבה | `ScanOrSearchModal.jsx`, `BarcodeDetector`, `useCameraStream` | יש לולאה אוטומטית כש־API נתמך, שדה ידני ו־fallback לצילום. |
| זיהוי מוצר במצלמה | ✅ קיים בכפוף לשרת | `/recognize-product`, `GEMINI_API_KEY` | צילום מפורש על ידי המשתמש; ללא מפתח/שרת תוצג שגיאה או fallback. |
| תמיכה במוצרים חדשים | ✅ קיים | `addProductToCatalog` + POST `/products` | המוצר הדינמי זמין לאחר טעינה/רענון קטלוג. |
| הזזה ברשימה | ✅ קיים | drag/drop + `reorderItems` | זמין ברשימה אישית בלבד; מושבת ב־trip. |
| שינוי כמות | ✅ קיים | `incrementItem`, `decrementItem` | ברשימה אישית בלבד. |
| הסרת מוצר | ✅ קיים | `removeItem` / `removeTripItem` | שני המסלולים שונים. |
| עלות מוצר וסכום | ✅ קיים עם גבול | `total` + `PriceTag` | פריטים אישיים אינם נכללים בסכום; מחיר חסר דורש בדיקת UI ייעודית. |
| מועדפים כללי/קיץ/חורף | ✅ קיים | `FavoritesManager.jsx`, `favorites.js` | נשמר מקומית; פריט שלא נמצא בקטלוג הופך ל־custom ולא ניתן לניווט. |
| כפתור ניווט | ✅ קיים | `onNavigate('nav')` ב־`Home.jsx` | דורש לבדוק בנפרד את חוזה הניווט במצב trip. |
| מעבר בין טאבים | ✅ קיים | `TABS` ו־`setTab` ב־`App.jsx` | ארבעה טאבים, ללא router. |

## בדיקה חיה שבוצעה

ב־localhost נפתח מסך ההתחברות ונצפו:

- כותרת SuperNav AI.
- כפתורי התחברות והרשמה.
- שדות שם משתמש וסיסמה.
- כפתור “שכחתי סיסמה”.

לא בוצעה התחברות בפועל כי אין חשבון בדיקה שסופק. לכן הזרימות שאחרי AuthGate מסומנות לפי ראיות קוד, ולא כבדיקה אינטראקטיבית מלאה.

## מצלמה וברקוד — הערכת סיכון

הקוד אכן מספק את הפיצ'ר שביקשת: כפתור יחיד בדף הבית פותח modal שמכוון את המצלמה למוצר או לברקוד. ברקוד מזוהה אוטומטית דרך `BarcodeDetector` כאשר הדפדפן תומך; אחרת יש צילום ברקוד ושדה ידני. זיהוי מוצר בתמונה נשלח ל־Gemini דרך השרת כאשר `GEMINI_API_KEY` מוגדר.

הבדיקה החיה לא אישרה צילום בפועל משום שאין הרשאת מצלמה שניתנה בסשן ואין חשבון מחובר. בנוסף, `getUserMedia` ו־`BarcodeDetector` דורשים secure context (HTTPS, כאשר localhost הוא חריג לפיתוח), ו־`BarcodeDetector` אינו נתמך בכל הדפדפנים. ראו [MDN BarcodeDetector](https://developer.mozilla.org/en-US/docs/Web/API/BarcodeDetector) ו־[MDN getUserMedia](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia).

## תקלות/פערים לפי עדיפות

### P1 — דף בית קבוצתי עדיין בסיסי

קיים `activeGroupId` ו־provider שמזין את `Home` ברשימת הקבוצה, אך עדיין אין WebSocket, היסטוריית פעילות או UX מלא בסגנון WhatsApp.

### P1 — הגבלת כמות עדיין חסרה

השרת אוכף הגבלת קטגוריה ומוצר בהוספה ובהעברה, אך אין עדיין שדה או כלל להגבלת מספר יחידות.

### P2 — UX של transfer אישי → קבוצה בסיסי

קיימת העברה אצווה עם רשימת דחויים, אך אין עדיין מסך אישור מפורט או בחירה פריט־פריט.

### P2 — כשלי API לא נראים מספיק בדף הבית

`useTripSync` שומר את הערך האחרון במקרה כשל, ו־Home אינו מציג banner שגיאה לפעולות הוספה/סימון/הסרה שנכשלו. המשתמש עלול לחשוב שהפעולה הצליחה.

### P2 — כיסוי QA אוטומטי חסר

קיים רק `src/lib/chainPriceBlocks.test.js`. אין Playwright/Cypress או בדיקות React לדף הבית, ולכן אין כרגע regression אוטומטי לזרימות login → profile → group → list → camera.

## מסקנה

דף הבית כולל כעת מצב אישי ומצב קבוצה בסיסי, עם רשימות ומועדפים נפרדים, סנכרון polling, העברה אצווה ואכיפת הרשאות קטגוריה/מוצר. עדיין חסרים הגבלת כמות, WebSocket, בחירה פריט־פריט ב־transfer ובדיקת מצלמה ידנית עם חשבון בדיקה והרשאת מצלמה.
